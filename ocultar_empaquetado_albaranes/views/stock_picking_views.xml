<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Heredar la vista de formulario de stock.picking para hacer la columna Empaquetado opcional -->
    <record id="view_picking_form_inherit_empaquetado_opcional" model="ir.ui.view">
        <field name="name">stock.picking.form.inherit.empaquetado.opcional</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"/>
        <field name="arch" type="xml">
            <!-- Modificar el campo result_package_id (Empaquetado) para hacerlo opcional -->
            <xpath expr="//field[@name='move_ids_without_package']/tree/field[@name='result_package_id']" position="attributes">
                <attribute name="optional">show</attribute>
            </xpath>
        </field>
    </record>
</odoo>
